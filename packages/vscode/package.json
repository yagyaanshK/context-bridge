{
  "name": "context-bridge-vscode",
  "displayName": "Context Bridge",
  "description": "Continue developer agent sessions across Claude, Codex, and other tools from VS Code.",
  "version": "0.2.1",
  "publisher": "yagyaanshK",
  "license": "MIT",
  "type": "commonjs",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:contextBridgeAccounts",
    "onCommand:contextBridge.switchAccount",
    "onCommand:contextBridge.addCodexAccount",
    "onCommand:contextBridge.importCodexAccount",
    "onCommand:contextBridge.refreshAccountQuota",
    "onCommand:contextBridge.discoverClaude",
    "onCommand:contextBridge.discoverCodex",
    "onCommand:contextBridge.importLatestClaude",
    "onCommand:contextBridge.importLatestCodex",
    "onCommand:contextBridge.handoffToClaudeExisting",
    "onCommand:contextBridge.handoffToClaudeNew",
    "onCommand:contextBridge.handoffToCodexExisting",
    "onCommand:contextBridge.handoffToCodexNew",
    "onCommand:contextBridge.openLatestHandoff",
    "onCommand:contextBridge.copyLatestHandoffPrompt"
  ],
  "main": "./src/extension.cjs",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "contextBridge",
          "title": "Context Bridge",
          "icon": "$(arrow-swap)"
        }
      ]
    },
    "views": {
      "contextBridge": [
        {
          "id": "contextBridgeAccounts",
          "name": "Codex Subscriptions",
          "contextualTitle": "Context Bridge"
        }
      ]
    },
    "commands": [
      {
        "command": "contextBridge.addCodexAccount",
        "title": "Context Bridge: Add Codex Account",
        "icon": "$(add)"
      },
      {
        "command": "contextBridge.importCodexAccount",
        "title": "Context Bridge: Import Current Codex Login"
      },
      {
        "command": "contextBridge.signInAccount",
        "title": "Context Bridge: Sign In",
        "icon": "$(sign-in)"
      },
      {
        "command": "contextBridge.refreshAccountQuota",
        "title": "Context Bridge: Refresh Subscription Quota",
        "icon": "$(refresh)"
      },
      {
        "command": "contextBridge.openAccountTerminal",
        "title": "Context Bridge: Open Codex Terminal for Account",
        "icon": "$(terminal)"
      },
      {
        "command": "contextBridge.switchAccount",
        "title": "Context Bridge: Switch Codex Subscription",
        "icon": "$(arrow-swap)"
      },
      {
        "command": "contextBridge.undoAccountSwitch",
        "title": "Context Bridge: Undo Codex Subscription Switch"
      },
      {
        "command": "contextBridge.showRawUsage",
        "title": "Context Bridge: Show Raw Usage Response",
        "icon": "$(json)"
      },
      {
        "command": "contextBridge.forgetAccount",
        "title": "Context Bridge: Remove Codex Account",
        "icon": "$(trash)"
      },
      {
        "command": "contextBridge.discoverClaude",
        "title": "Context Bridge: Discover Claude Sessions"
      },
      {
        "command": "contextBridge.discoverCodex",
        "title": "Context Bridge: Discover Codex Sessions"
      },
      {
        "command": "contextBridge.importLatestClaude",
        "title": "Context Bridge: Import Latest Claude Session"
      },
      {
        "command": "contextBridge.importLatestCodex",
        "title": "Context Bridge: Import Latest Codex Session"
      },
      {
        "command": "contextBridge.handoffToClaudeExisting",
        "title": "Context Bridge: Handoff to Existing Claude Session"
      },
      {
        "command": "contextBridge.handoffToClaudeNew",
        "title": "Context Bridge: Handoff to New Claude Session"
      },
      {
        "command": "contextBridge.handoffToCodexExisting",
        "title": "Context Bridge: Handoff to Existing Codex Session"
      },
      {
        "command": "contextBridge.handoffToCodexNew",
        "title": "Context Bridge: Handoff to New Codex Session"
      },
      {
        "command": "contextBridge.openLatestHandoff",
        "title": "Context Bridge: Open Latest Handoff"
      },
      {
        "command": "contextBridge.copyLatestHandoffPrompt",
        "title": "Context Bridge: Copy Latest Handoff Prompt"
      }
    ],
    "configuration": {
      "title": "Context Bridge",
      "properties": {
        "contextBridge.maxExportChars": {
          "type": "number",
          "default": 120000,
          "description": "Character budget for the transcript embedded in a handoff. Receiving agents refuse or silently truncate oversized files, so an unbounded handoff delivers less than a budgeted one. User turns are reserved first, then the most recent remaining turns fill the budget; anything dropped is reported in the handoff header. 0 disables clipping."
        },
        "contextBridge.sinceLastExport": {
          "type": "boolean",
          "default": false,
          "description": "Only include turns recorded after the previous export. Keeps repeat handoffs small, but a new agent session has no memory of what an earlier handoff delivered, so leave this off unless you always paste every handoff into the same continuing session."
        },
        "contextBridge.dedupeTurns": {
          "type": "boolean",
          "default": true,
          "description": "Collapse consecutive duplicate turns. Native logs (e.g. Codex) record each message under several event types, producing 2-3 identical copies; this removes the repeats."
        },
        "contextBridge.toolMaxChars": {
          "type": "number",
          "default": 2000,
          "description": "Truncate tool-output turns longer than this many characters (keeping head and tail). 0 disables truncation. Tool outputs like git diffs and directory listings dominate handoff size."
        },
        "contextBridge.systemMaxChars": {
          "type": "number",
          "default": 800,
          "description": "Truncate system turns (e.g. repeated turn-context blobs) longer than this many characters. 0 disables truncation."
        },
        "contextBridge.snapshotDiffMaxChars": {
          "type": "number",
          "default": 4000,
          "description": "How much of the uncommitted diff (staged and unstaged, vs HEAD) to include in the handoff. A handoff taken mid-edit is exactly when this matters. 0 omits the diff body; the file-level stat is still shown."
        },
        "contextBridge.keepExports": {
          "type": "number",
          "default": 10,
          "description": "How many past handoff files to keep in .context-bridge/exports. Older ones are deleted after each export. 0 keeps all of them."
        },
        "contextBridge.openHandoffDocument": {
          "type": "boolean",
          "default": true,
          "description": "Open the generated handoff markdown document after export."
        },
        "contextBridge.claudeUri": {
          "type": "string",
          "default": "vscode://anthropic.claude-code/open",
          "description": "Optional URI used to open or focus Claude Code when contextBridge.allowExternalClaudeUri is enabled."
        },
        "contextBridge.allowExternalClaudeUri": {
          "type": "boolean",
          "default": false,
          "description": "Allow Context Bridge to open contextBridge.claudeUri. Keep false in VS Code forks so handoff stays in the current editor."
        },
        "contextBridge.claudeOpenCommand": {
          "type": "string",
          "default": "",
          "description": "Optional VS Code command id used to open or focus Claude. If empty, Context Bridge tries to find a command containing 'claude' or 'anthropic'."
        },
        "contextBridge.codexOpenCommand": {
          "type": "string",
          "default": "",
          "description": "Optional VS Code command id used to open or focus Codex. If empty, Context Bridge tries to find a command containing 'codex'."
        }
      }
    },
    "menus": {
      "view/title": [
        {
          "command": "contextBridge.addCodexAccount",
          "when": "view == contextBridgeAccounts",
          "group": "navigation@1"
        },
        {
          "command": "contextBridge.refreshAccountQuota",
          "when": "view == contextBridgeAccounts",
          "group": "navigation@2"
        },
        {
          "command": "contextBridge.importCodexAccount",
          "when": "view == contextBridgeAccounts",
          "group": "1_add"
        }
      ],
      "view/item/context": [
        {
          "command": "contextBridge.switchAccount",
          "when": "view == contextBridgeAccounts && viewItem == contextBridgeCodexAccount",
          "group": "inline@1"
        },
        {
          "command": "contextBridge.openAccountTerminal",
          "when": "view == contextBridgeAccounts && viewItem =~ /contextBridgeCodexAccount/",
          "group": "inline@2"
        },
        {
          "command": "contextBridge.signInAccount",
          "when": "view == contextBridgeAccounts && viewItem =~ /contextBridgeCodexAccount/",
          "group": "1_use@1"
        },
        {
          "command": "contextBridge.showRawUsage",
          "when": "view == contextBridgeAccounts && viewItem =~ /contextBridgeCodexAccount/",
          "group": "2_debug"
        },
        {
          "command": "contextBridge.forgetAccount",
          "when": "view == contextBridgeAccounts && viewItem =~ /contextBridgeCodexAccount/",
          "group": "9_danger"
        },
        {
          "command": "contextBridge.refreshAccountQuota",
          "when": "view == contextBridgeAccounts && viewItem == contextBridgePooled",
          "group": "inline@1"
        }
      ],
      "commandPalette": [
        {
          "command": "contextBridge.handoffToClaudeExisting"
        },
        {
          "command": "contextBridge.handoffToClaudeNew"
        },
        {
          "command": "contextBridge.handoffToCodexExisting"
        },
        {
          "command": "contextBridge.handoffToCodexNew"
        },
        {
          "command": "contextBridge.discoverClaude"
        },
        {
          "command": "contextBridge.discoverCodex"
        }
      ]
    }
  },
  "dependencies": {
    "@context-bridge/core": "0.2.1"
  },
  "devDependencies": {
    "@types/vscode": "^1.95.0"
  }
}
